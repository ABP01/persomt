type Props ={
    href: string;
    label: string;
    isActive: boolean;
};

export const NavButton = ({}: Props) => {
    return (
        <div>
            <h1>NavButton</h1>
        </div>
    );
}